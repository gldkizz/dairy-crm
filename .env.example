# Local development (npm run dev + docker compose -f docker-compose.dev.yml)
DATABASE_URL="postgresql://crm:crm@localhost:5432/dairy_crm?schema=public"
AUTH_SECRET="dairy-crm-dev-secret-change-in-production"
APP_URL=http://localhost:3000
AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Production behind nginx on :8080 (docker compose.yml full stack):
# APP_URL=http://82.39.215.140:8080
# AUTH_URL / NEXTAUTH_URL are derived from APP_URL in compose for the app container.
